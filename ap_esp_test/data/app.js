/*
const sbmBtn = document.getElementById("sbmBtn");
const porukaEl = document.getElementById("poruka");
const ponavljanjeEl = document.getElementById("ponavljanje");

sbmBtn.addEventListener("submit", e => {
    e.preventDefault();
    console.log("cao");
    porukaEl.value = "";
    ponavljanjeEl.value = false;
})*/
/*
const formEl = document.getElementById("forma");
    
formEl.addEventListener("submit", e => {
    e.preventDefault();

    const formData = new FormData(e.currentTarget);
    const params = new URLSearchParams(formData).toString();
        
    fetch(`/submit?${params}`)
        .then(response => {
        // Immediately redirect to "/"
        window.location.reload();
    })
});
*/